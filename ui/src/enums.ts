export enum contentLinksEnum {
  DASHBOARD = "dashboard",
  SAVED = "saved",
  PROFILE = "profile",
  CONTACT = "contact",
}

export enum petTypesEnum {
  DOG = "Dog",
  CAT = "Cat",
}

export enum adoptionStatusEnum {
  AVAILABLE = "Available",
  ADOPTED = "Adopted",
  FOSTERED = "Fostered",
}

export enum adoptionTypeEnum {
  ADOPTED = "Adopted",
  FOSTERED = "Fostered",
}
